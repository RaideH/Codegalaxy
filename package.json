{
  "name": "codegalaxy",
  "version": "1.0.0",
  "private": true,
  "description": "Educational static site for learning HTML/CSS/JS",
  "scripts": {
    "start": "serve . -l 5000",
    "dev": "live-server --port=3000 --open=./index.html",
    "http": "python -m http.server 8000",
    "build": "echo \"No build step configured\"",
    "format": "prettier --write \"**/*.{html,css,js,md}\""
  }
}
